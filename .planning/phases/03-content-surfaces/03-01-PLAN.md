---
phase: 03-content-surfaces
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - tailwind.config.ts
  - components/StoryCard.tsx
autonomous: true
requirements:
  - CONT-01
  - CONT-04
  - CONT-05
must_haves:
  truths:
    - "StoryCard renders headlines using text-subheading token — no text-[19px] or text-[21px] remains"
    - "StoryCard topic label uses text-label token with explicit tracking-[0.12em] — not .section-label"
    - "StoryCard excerpt uses text-caption token — no text-[13px] remains"
    - "StoryCard shows border colour shift on hover (hover:border-stone-300 dark:hover:border-stone-600) alongside bg shift"
    - "tailwind.config.ts has an article fontSize entry at 1.75rem with lineHeight 1.2 and fontWeight 700"
    - "No text-[19px], text-[21px], or text-[13px] arbitrary sizes remain in StoryCard.tsx"
    - "No hover:opacity on StoryCard — transition-opacity replaced with transition-colors"
  artifacts:
    - path: "tailwind.config.ts"
      provides: "text-article token (1.75rem/28px) added to fontSize extend block"
      contains: "article: ['1.75rem'"
    - path: "components/StoryCard.tsx"
      provides: "Token-migrated card component with semantic hover states"
  key_links:
    - from: "tailwind.config.ts"
      to: "components/StoryCard.tsx"
      via: "text-subheading, text-caption, text-label tokens"
      pattern: "text-subheading|text-caption|text-label"
    - from: "components/StoryCard.tsx"
      to: "hover state"
      via: "border colour shift on hover"
      pattern: "hover:border-stone-300"
---

<objective>
Add the text-article fontSize token to tailwind.config.ts and migrate StoryCard.tsx to use named type scale tokens with semantic hover states.

Purpose: StoryCard is the highest-visibility component — seen 8 times per page visit by every user. Token migration here fulfils CONT-01 (StoryCard polished) and begins CONT-05 (typographic consistency). The text-article token addition is needed by plan 03-02 (ArticleStory).

Output: Updated tailwind.config.ts with text-article token; updated StoryCard.tsx with named tokens and border hover state.
</objective>

<execution_context>
@/Users/gregoryvitrenko/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gregoryvitrenko/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Current token system from tailwind.config.ts — executor uses these directly -->

From tailwind.config.ts (theme.extend.fontSize — already established in Phase 1):
```typescript
display:    ['2.25rem',   { lineHeight: '1.2',  fontWeight: '700' }],   // 36px
heading:    ['1.5rem',    { lineHeight: '1.25', fontWeight: '600' }],   // 24px
subheading: ['1.125rem',  { lineHeight: '1.35', fontWeight: '500' }],   // 18px — note: fontWeight 500, keep explicit font-bold for card headlines
body:       ['0.9375rem', { lineHeight: '1.6'  }],                      // 15px
caption:    ['0.8125rem', { lineHeight: '1.4'  }],                      // 13px
label:      ['0.625rem',  { lineHeight: '1', letterSpacing: '0.1em', fontWeight: '500' }], // 10px
```

New token to add (after existing label entry):
```typescript
article: ['1.75rem', { lineHeight: '1.2', fontWeight: '700' }],   // 28px — article-level headline
```

From components/StoryCard.tsx — current arbitrary sizes to replace:
```
text-[19px] sm:text-[21px]        → text-subheading + keep font-bold (subheading is 500, card headlines are bold)
text-[13px] (excerpt)             → text-caption
text-[10px] tracking-[0.12em]     → text-label + keep explicit tracking-[0.12em] (NOT .section-label — different font/tracking)
text-[10px] (firm chip)           → text-label
text-[11px] (teaser italic)       → text-label (round down 1px — acceptable for de-emphasised secondary text)
text-[11px] (read more)           → text-label
```

From components/StoryCard.tsx — current hover state (line 36):
```typescript
// Current (missing border hover):
"relative flex flex-col bg-white dark:bg-stone-900 border border-stone-200 dark:border-stone-800 rounded-sm overflow-hidden px-5 pt-5 pb-6 h-full transition-colors hover:bg-stone-50 dark:hover:bg-stone-800/40"

// After (add border hover):
"relative flex flex-col bg-white dark:bg-stone-900 border border-stone-200 dark:border-stone-800 rounded-sm overflow-hidden px-5 pt-5 pb-6 h-full transition-colors hover:bg-stone-50 dark:hover:bg-stone-800/40 hover:border-stone-300 dark:hover:border-stone-600"
```

Established patterns from Phase 2:
- bg-paper is shell-only — cards stay bg-white dark:bg-stone-900 (locked decision)
- Token migration: replace arbitrary text-[Npx] with named token, remove responsive pair
- Keep font-bold explicitly when token fontWeight is lighter than needed (subheading = 500, card headlines = 700)
- Do NOT use .section-label for topic labels (that class is font-mono; topic labels are font-sans with tracking-[0.12em])
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add text-article token to tailwind.config.ts</name>
  <files>tailwind.config.ts</files>
  <action>
    In the theme.extend.fontSize block, add the article token as the last entry after label:

    ```typescript
    article: ['1.75rem', { lineHeight: '1.2', fontWeight: '700' }],   // 28px — article-level headline
    ```

    Place it immediately after the label entry. No other changes to this file.

    After editing, run `npm run build` to confirm the new text-article utility is available and TypeScript compiles cleanly.
  </action>
  <verify>
    <automated>cd /Users/gregoryvitrenko/Documents/Folio && grep "article:" tailwind.config.ts && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>tailwind.config.ts contains `article: ['1.75rem', { lineHeight: '1.2', fontWeight: '700' }]` and npm run build succeeds without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate StoryCard.tsx to design tokens with semantic hover</name>
  <files>components/StoryCard.tsx</files>
  <action>
    Make the following targeted changes to StoryCard.tsx. No structural changes, no prop API changes.

    **1. Article element (line 36) — add border hover:**
    Current: `transition-colors hover:bg-stone-50 dark:hover:bg-stone-800/40`
    Replace with: `transition-colors hover:bg-stone-50 dark:hover:bg-stone-800/40 hover:border-stone-300 dark:hover:border-stone-600`

    **2. Topic label span (line 53) — migrate text size:**
    Current: `text-[10px] font-sans font-semibold tracking-[0.12em] uppercase`
    Replace with: `text-label font-sans font-semibold tracking-[0.12em] uppercase`
    (Keep explicit tracking-[0.12em] — topic labels have tighter tracking than .section-label's tracking-widest. Do NOT use .section-label here.)

    **3. Headline h2 (line 59) — migrate text size, remove responsive pair:**
    Current: `font-serif text-[19px] sm:text-[21px] font-bold leading-snug`
    Replace with: `font-serif text-subheading font-bold leading-snug`
    (Keep font-bold explicitly — text-subheading has fontWeight 500, card headlines must be bold)

    **4. Excerpt paragraph (line 64) — migrate text size:**
    Current: `text-[13px] text-stone-500`
    Replace with: `text-caption text-stone-500`

    **5. Firm chip classes (lines 74, 80-81) — migrate text size:**
    Current: `text-[10px] font-sans font-medium`
    Replace with: `text-label font-sans font-medium`
    (Both the base chipClass string on line 74 and the hover Link variant use text-[10px] — update the chipClass string definition only, it applies to both)

    **6. Interview angle teaser paragraph (line 95) — migrate text size:**
    Current: `text-[11px] italic text-stone-400`
    Replace with: `text-label italic text-stone-400`

    **7. Read more paragraph (line 101) — migrate text size:**
    Current: `text-[11px] font-sans font-medium tracking-wide`
    Replace with: `text-label font-sans font-medium tracking-wide`

    After all changes, run: `grep 'text-\[' components/StoryCard.tsx` — should return no matches (or only non-font-size brackets, which is fine). Then run `npm run build`.

    Anti-patterns to avoid:
    - Do NOT change bg-white dark:bg-stone-900 — cards stay on white surfaces (locked decision)
    - Do NOT apply .section-label to topic labels (wrong font family and tracking)
    - Do NOT remove font-bold from the headline h2 (subheading token is fontWeight 500, not 700)
    - Do NOT change TOPIC_STYLES classes — those are dynamic and untouched
  </action>
  <verify>
    <automated>cd /Users/gregoryvitrenko/Documents/Folio && grep 'text-\[' components/StoryCard.tsx; grep 'hover:opacity' components/StoryCard.tsx; npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - No `text-[Npx]` arbitrary font sizes remain in StoryCard.tsx
    - No `hover:opacity` in StoryCard.tsx
    - `hover:border-stone-300 dark:hover:border-stone-600` is present on the article element
    - `text-subheading font-bold` is on the headline h2 (with no sm: breakpoint variant)
    - `text-caption` is on the excerpt paragraph
    - `text-label` is on topic labels, firm chips, teaser, and read more
    - `npm run build` succeeds
  </done>
</task>

</tasks>

<verification>
After both tasks complete:

```bash
# 1. Confirm no arbitrary font sizes remain in StoryCard
grep 'text-\[' /Users/gregoryvitrenko/Documents/Folio/components/StoryCard.tsx

# 2. Confirm no hover:opacity in StoryCard
grep 'hover:opacity' /Users/gregoryvitrenko/Documents/Folio/components/StoryCard.tsx

# 3. Confirm text-article token exists
grep 'article:' /Users/gregoryvitrenko/Documents/Folio/tailwind.config.ts

# 4. Full build clean
cd /Users/gregoryvitrenko/Documents/Folio && npm run build
```

Expected: grep commands 1 and 2 return no matches; grep 3 finds the article entry; build succeeds.
</verification>

<success_criteria>
- `tailwind.config.ts` has `article: ['1.75rem', { lineHeight: '1.2', fontWeight: '700' }]` in fontSize extend
- `StoryCard.tsx` has zero `text-[Npx]` arbitrary font sizes
- `StoryCard.tsx` has zero `hover:opacity` patterns
- `StoryCard.tsx` article element includes `hover:border-stone-300 dark:hover:border-stone-600`
- `StoryCard.tsx` h2 headline uses `text-subheading font-bold` (single non-responsive value)
- `npm run build` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/03-content-surfaces/03-01-SUMMARY.md` using the summary template.
</output>
