---
phase: 04-conversion-surfaces
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - components/LandingHero.tsx
autonomous: true
requirements:
  - CONV-04

must_haves:
  truths:
    - "The LandingHero CTA uses rounded-chrome (4px) not rounded-xl"
    - "Stage selector buttons use rounded-chrome not rounded-lg"
    - "The recommendation panel uses rounded-card not rounded-xl"
    - "The CTA hover uses the established bg-stone-700 pattern, not hover:opacity"
    - "No arbitrary text-[Npx] sizes remain in LandingHero"
  artifacts:
    - path: "components/LandingHero.tsx"
      provides: "Token-compliant hero component"
      contains: "rounded-chrome"
  key_links:
    - from: "components/LandingHero.tsx"
      to: "/sign-up"
      via: "Link href on CTA"
      pattern: "sign-up"
---

<objective>
Migrate components/LandingHero.tsx to design tokens: fix the rounded-xl CTA radius, apply rounded-chrome to all button/chrome elements, apply rounded-card to the recommendation panel, replace arbitrary text-[Npx] sizes with named tokens, and fix the hover:opacity on the CTA.

Purpose: The hero is the first thing free users see. After this plan, every interactive element in the hero uses the token system established in Phases 1–3 — no visual regression, just token consistency.

Output: A fully updated components/LandingHero.tsx with no rounded-xl, no rounded-lg, no hover:opacity, and no arbitrary text-[Npx] values.
</objective>

<execution_context>
@/Users/gregoryvitrenko/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gregoryvitrenko/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-conversion-surfaces/04-CONTEXT.md
@.planning/phases/04-conversion-surfaces/04-RESEARCH.md

<interfaces>
<!-- Key tokens and patterns for LandingHero migration. All confirmed in codebase. -->

From tailwind.config.ts (confirmed tokens):
- text-caption  → 13px (replaces text-[13px] instances)
- text-label    → 10px (replaces text-[12px] instances)
- rounded-card  → 2px editorial card radius (replaces rounded-xl on panels)
- rounded-chrome → 4px button/chrome radius (replaces rounded-xl and rounded-lg on buttons/links)

NOTE on rounded-lg vs rounded-chrome: In this project, --radius was reduced to 0.25rem in Phase 1.
Tailwind's rounded-lg currently resolves to 0.25rem (4px), same as rounded-chrome. They are visually
identical NOW but rounded-chrome is the semantic token and the locked decision — use it for correctness.

From app/globals.css:
- .section-label encodes: font-mono text-label tracking-widest uppercase text-stone-400 dark:text-stone-500
  Apply alone — do NOT add text-stone-400 alongside it (already encoded).

Established hover patterns (Phase 3):
- Dark-bg CTA: hover:bg-stone-700 dark:hover:bg-stone-300 transition-colors (NOT hover:opacity)

Hero headline: text-3xl sm:text-4xl — leave as responsive pair. 30/36px straddles tokens.
Stage selector "Where are you" intro line: text-[13px] → text-caption (this line is NOT in locked decisions but consistent with token intent — migrate it).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply radius tokens, text tokens, and hover fix to LandingHero</name>
  <files>components/LandingHero.tsx</files>
  <action>
Apply all changes to components/LandingHero.tsx. Make changes in this exact order:

1. TOP LABEL (line 55):
   - Replace: `"font-mono text-[10px] tracking-widest uppercase text-stone-400 dark:text-stone-500 mb-4"`
   - With: `"section-label mb-4"`
   - Do NOT add text-stone-400 alongside section-label — it's already encoded in the class.

2. STAGE SELECTOR intro text (line 65):
   - Replace: `"text-[13px] text-stone-500 dark:text-stone-400 mb-3"`
   - With: `"text-caption text-stone-500 dark:text-stone-400 mb-3"`

3. STAGE SELECTOR buttons (line 73):
   - In the className template literal, replace: `rounded-lg text-[13px]`
   - With: `rounded-chrome text-caption`
   - The rest of the conditional class string (bg, text colour, border states) stays unchanged.

4. RECOMMENDATION PANEL container (line 86):
   - Replace: `"mb-6 p-4 rounded-xl bg-stone-50 dark:bg-stone-800/50 border border-stone-200 dark:border-stone-700"`
   - With: `"mb-6 p-4 rounded-card bg-stone-50 dark:bg-stone-800/50 border border-stone-200 dark:border-stone-700"`

5. RECOMMENDATION HEADING (line 87):
   - Replace: `"text-[13px] font-semibold text-stone-800 dark:text-stone-200 mb-1"`
   - With: `"text-caption font-semibold text-stone-800 dark:text-stone-200 mb-1"`

6. RECOMMENDATION DESCRIPTION (line 90):
   - Replace: `"text-[13px] text-stone-500 dark:text-stone-400 mb-3"`
   - With: `"text-caption text-stone-500 dark:text-stone-400 mb-3"`

7. RECOMMENDATION LINKS (line 98):
   - Replace: `rounded-lg` → `rounded-chrome`
   - Replace: `text-[12px]` → `text-label`
   - The rest of the className stays unchanged.

8. CTA LINK (line 112):
   - Replace: `"inline-block px-6 py-2.5 rounded-xl bg-stone-900 dark:bg-stone-100 text-stone-50 dark:text-stone-900 text-[14px] font-medium hover:opacity-80 transition-opacity"`
   - With: `"inline-block px-6 py-2.5 rounded-chrome bg-stone-900 dark:bg-stone-100 text-stone-50 dark:text-stone-900 text-caption font-medium hover:bg-stone-700 dark:hover:bg-stone-300 transition-colors"`

9. FREE-TIER HINT (line 116):
   - Replace: `"text-[12px] text-stone-400 dark:text-stone-500"`
   - With: `"text-label text-stone-400 dark:text-stone-500"`

10. HERO HEADLINE (line 60): Leave `text-3xl sm:text-4xl` completely unchanged — no clean single token mapping exists for the 30/36px responsive range.

Do NOT modify: the Stage type, STAGES array, STAGE_CONTENT data, useState logic, Link href values, icon sizes, or any non-className attributes.
  </action>
  <verify>
    <automated>grep -n "rounded-xl\|rounded-lg\|hover:opacity\|text-\[" /Users/gregoryvitrenko/Documents/Folio/components/LandingHero.tsx</automated>
  </verify>
  <done>grep returns zero matches. All rounded-xl, rounded-lg, hover:opacity, and arbitrary text-[Npx] values are replaced. The hero headline text-3xl sm:text-4xl pair is the only intentional exception and will not appear in grep output (those are not text-[ patterns).</done>
</task>

</tasks>

<verification>
After task completes:

1. No rounded-xl: `grep -c "rounded-xl" /Users/gregoryvitrenko/Documents/Folio/components/LandingHero.tsx` returns 0
2. No rounded-lg: `grep -c "rounded-lg" /Users/gregoryvitrenko/Documents/Folio/components/LandingHero.tsx` returns 0
3. No hover:opacity: `grep -c "hover:opacity" /Users/gregoryvitrenko/Documents/Folio/components/LandingHero.tsx` returns 0
4. No arbitrary text sizes: `grep -c "text-\[" /Users/gregoryvitrenko/Documents/Folio/components/LandingHero.tsx` returns 0
5. Tokens applied: `grep -c "rounded-chrome" /Users/gregoryvitrenko/Documents/Folio/components/LandingHero.tsx` returns 3 (stage buttons, recommendation links, CTA)
6. CTA hover correct: `grep "hover:bg-stone-700" /Users/gregoryvitrenko/Documents/Folio/components/LandingHero.tsx` returns 1 match
</verification>

<success_criteria>
- CTA Link uses rounded-chrome (not rounded-xl)
- Stage selector buttons use rounded-chrome (not rounded-lg)
- Recommendation panel uses rounded-card (not rounded-xl)
- Recommendation links use rounded-chrome (not rounded-lg)
- CTA hover uses hover:bg-stone-700 dark:hover:bg-stone-300 transition-colors (not hover:opacity)
- Top label uses .section-label class (not manual font-mono text-[10px] tracking-widest)
- All text-[Npx] instances replaced with named tokens
- Hero headline text-3xl sm:text-4xl left unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/04-conversion-surfaces/04-02-SUMMARY.md`
</output>
