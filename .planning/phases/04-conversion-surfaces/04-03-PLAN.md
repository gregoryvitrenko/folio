---
phase: 04-conversion-surfaces
plan: "03"
type: execute
wave: 2
depends_on:
  - "04-01"
  - "04-02"
files_modified: []
autonomous: false
requirements:
  - CONV-01
  - CONV-02
  - CONV-03
  - CONV-04

must_haves:
  truths:
    - "Upgrade page reads as the same visual product as the briefing pages in both light and dark mode"
    - "Social proof block is visible on upgrade page with no layout issues"
    - "LandingHero CTA has correct rounded-chrome radius — visually flat, not pill-shaped"
    - "Stripe checkout initiates without error from the upgrade page"
  artifacts:
    - path: "app/upgrade/page.tsx"
      provides: "Verified upgrade page (plans 04-01 tasks complete)"
    - path: "components/LandingHero.tsx"
      provides: "Verified LandingHero component (plan 04-02 task complete)"
  key_links:
    - from: "Upgrade page CTA button"
      to: "Stripe checkout"
      via: "handleUpgrade → /api/stripe/checkout POST"
      pattern: "checkout"
---

<objective>
Visual and functional verification of all Phase 4 changes. Both files have been updated by plans 04-01 and 04-02 — this checkpoint confirms the changes look and function correctly before the phase is marked complete.

Purpose: CSS/copy changes cannot be fully verified by grep alone. Visual inspection in browser (light + dark, mobile + desktop) catches contrast issues, layout breaks, and interaction regressions. STATE.md requires a Stripe checkout smoke test for any deploy touching /upgrade.

Output: User confirmation that Phase 4 changes are visually correct and functionally sound.
</objective>

<execution_context>
@/Users/gregoryvitrenko/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gregoryvitrenko/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/04-conversion-surfaces/04-01-SUMMARY.md
@.planning/phases/04-conversion-surfaces/04-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Visual and functional verification of Phase 4 changes</name>
  <action>
Deploy to production first (push to main, wait for Vercel build). Then verify the following in order:

What was built:
- Plan 04-01: app/upgrade/page.tsx — zinc→stone palette, design tokens, outcome copy, social proof block, SiteFooter
- Plan 04-02: components/LandingHero.tsx — rounded-chrome, text tokens, hover:bg fix

Verification steps:

1. Upgrade page — light mode (folioapp.co.uk/upgrade)
   - Stone palette throughout — no coolish-grey zinc tones, everything is warm stone
   - "Premium" label is mono + uppercase + stone-400 colour
   - Feature list: six outcome-leading strings. First item starts "Walk into interviews with a point of view"
   - Social proof block between features card and free-tier note: "Join 200+ law students" badge + quote + attribution
   - CTA button: warm stone-900 bg, hover shifts to stone-700 (slightly lighter warm dark), NOT translucent
   - Reassurance "Cancel any time" text visible and correct size
   - SiteFooter appears at the bottom of the page

2. Upgrade page — dark mode (toggle in header)
   - Stone-100 CTA bg in dark mode, hover shifts to stone-300
   - Social proof quote readable against dark background (stone-400)
   - Features card bg is stone-900 (warm dark, not cold zinc-900)
   - Free-tier note bg is stone-800/40 (warm semi-transparent)

3. Upgrade page — mobile (resize to 375px or DevTools)
   - No layout breaks, text legible, CTA button full width
   - Social proof block centred and readable

4. Homepage hero — LandingHero (folioapp.co.uk)
   - CTA button "Get started — £4/month →" has a flat radius (4px — barely visible curve, not rounded)
   - Stage selector buttons (First year / VS / TC) have same flat radius
   - Click a stage button — recommendation panel appears with flat-radius card
   - CTA hover: bg darkens to stone-700 (NOT opacity fade)

5. Stripe checkout smoke test (open incognito window)
   - Visit folioapp.co.uk/upgrade
   - Click "Create account & subscribe →" (or "Subscribe — £4/month →" if signed in)
   - Verify redirect to Stripe checkout page without error
   - You do NOT need to complete payment — just confirm the redirect works
   - STATE.md requires this smoke test for any deploy touching /upgrade
  </action>
  <verify>User visual inspection and Stripe redirect test in incognito</verify>
  <done>User types "approved" confirming all five steps pass — stone palette correct in light and dark, social proof visible, LandingHero flat radius, Stripe checkout redirect works</done>
  <what-built>
    Plan 04-01: app/upgrade/page.tsx — zinc→stone palette, design tokens, outcome copy, social proof block, SiteFooter
    Plan 04-02: components/LandingHero.tsx — rounded-chrome, text tokens, hover:bg fix
  </what-built>
  <how-to-verify>
    Deploy to production (push to main). Then follow the 5 verification steps in the action above.
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
This plan IS the verification step. No automated checks — visual + functional only.

Acceptance: User types "approved" after completing all five verification steps above.
</verification>

<success_criteria>
- Upgrade page is visually consistent with the rest of the product (warm stone, not zinc)
- Social proof block visible and correctly placed
- LandingHero CTA has flat radius and correct hover state
- Stripe checkout redirect works from /upgrade in incognito
- No layout breaks at 375px mobile
</success_criteria>

<output>
After completion, create `.planning/phases/04-conversion-surfaces/04-03-SUMMARY.md`
</output>
