---
phase: 23-events-podcast-accent
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/events/CityFilter.tsx
autonomous: true
requirements:
  - EVENTS-01

must_haves:
  truths:
    - "Event cards have rounded-3xl corners (rounded-card) — matching the v2 design system"
    - "The active city filter tab uses Oxford blue background with white text instead of the current underline/stone treatment"
    - "The date displayed on each event card uses Oxford blue as a text accent colour"
  artifacts:
    - path: "app/events/CityFilter.tsx"
      provides: "Event card and city filter tab rendering"
      contains: "bg-\\[#002147\\]"
  key_links:
    - from: "CityFilter.tsx tabActive"
      to: "Oxford blue active state"
      via: "bg-[#002147] text-white className"
      pattern: "bg-\\[#002147\\]"
    - from: "EventCard date span"
      to: "Oxford blue date accent"
      via: "text-[#002147] dark:text-blue-300 className"
      pattern: "text-\\[#002147\\]"
---

<objective>
Update the events page city filter tabs and event card date accents to use Oxford blue (#002147), and confirm event card corners use rounded-card — aligning the events page with the v2 design system.

Purpose: The events page is one of the last pages not yet updated to the v2 rounded/Oxford blue aesthetic. This plan closes that gap.
Output: Updated app/events/CityFilter.tsx with Oxford blue active tab state and date accent.
</objective>

<execution_context>
@/Users/gregoryvitrenko/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gregoryvitrenko/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Design constraints from CLAUDE.md and v2 decisions:
- Oxford blue: `#002147`
- `rounded-card` for cards (already applied via `rounded-card` class in EventCard — confirm it maps to 24px from Phase 20 tailwind config)
- `.section-label` for overline labels
- Stone palette for content, zinc for UI chrome only
- No emojis, lucide-react for icons
- `rounded-chrome` for chips/badges

<interfaces>
From app/events/CityFilter.tsx (current implementation):

```typescript
// City filter tab classes (current):
const tabBase = 'pb-2 text-label font-medium whitespace-nowrap border-b-2 -mb-px transition-colors cursor-pointer bg-transparent border-none outline-none font-sans';
const tabActive = 'border-stone-900 dark:border-stone-100 text-stone-900 dark:text-stone-100';
const tabInactive = 'border-transparent text-stone-400 dark:text-stone-500 hover:text-stone-700 dark:hover:text-stone-300';

// EventCard date display (current):
<p className="text-caption text-stone-500 mb-1">
  {formatShortDate(event.date, event.time)}
</p>
```

Types from lib/types.ts (expected):
```typescript
type EventCity = 'London' | 'Manchester' | 'Edinburgh' | 'Bristol' | 'Other';
type EventType = 'Networking' | 'Panel' | 'Workshop' | 'Social' | 'Career Fair';
interface LegalEvent {
  id: string;
  title: string;
  date: string;        // YYYY-MM-DD
  time?: string;
  city: EventCity;
  eventType: EventType;
  organiser: string;
  eligibility: string;
  url?: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Oxford blue active tabs and date accent in CityFilter</name>
  <files>app/events/CityFilter.tsx</files>
  <action>
Make three targeted changes to CityFilter.tsx:

1. **Active city filter tab** — Replace the current underline-based active style with an Oxford blue pill/button treatment:
   - Change `tabActive` from `'border-stone-900 dark:border-stone-100 text-stone-900 dark:text-stone-100'`
   - To `'border-transparent bg-[#002147] text-white rounded-full px-3'`
   - Also update `tabBase` to remove the `border-b-2 -mb-px pb-2` treatment since active tabs now use a pill — use `py-1.5` instead of `pb-2`
   - The inactive tabs remain text-only with hover state (no underline needed when active is pill-shaped)
   - New `tabBase`: `'text-label font-medium whitespace-nowrap transition-colors cursor-pointer bg-transparent border-none outline-none font-sans py-1.5 px-3 rounded-full'`
   - New `tabActive`: `'bg-[#002147] text-white'`
   - New `tabInactive`: `'text-stone-400 dark:text-stone-500 hover:text-stone-700 dark:hover:text-stone-300 hover:bg-stone-100 dark:hover:bg-stone-800'`
   - Remove the bottom border separator div (`border-b border-stone-200 dark:border-stone-800`) from the tab container — replace with `gap-2 flex-wrap` layout (no hard underline needed)
   - The fade gradient at the right edge (`bg-gradient-to-l from-stone-50`) can remain for overflow scroll cue

2. **Event card date accent** — In `EventCard`, change the date `<p>` from `text-stone-500` to `text-[#002147] dark:text-blue-300` so the date reads as an Oxford blue accent:
   - Change: `<p className="text-caption text-stone-500 mb-1">`
   - To: `<p className="text-caption text-[#002147] dark:text-blue-300 font-medium mb-1">`

3. **Event card radius** — The `EventCard` `<Link>` already uses `rounded-card`. Verify this is present and leave it unchanged — do not switch to `rounded-3xl` inline (the `rounded-card` CSS variable set in Phase 20 already maps to 24px).

Do NOT change: event type colour chips, event card border, hover states on cards, EventsGrid layout, the empty state rendering, or app/events/page.tsx.
  </action>
  <verify>
    <automated>npx tsc --noEmit --project /Users/gregoryvitrenko/Documents/Folio/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>
    - TypeScript compiles with no errors
    - CityFilter.tsx contains `bg-[#002147]` for active tab state
    - CityFilter.tsx contains `text-[#002147]` for date accent on EventCard
    - EventCard Link element still has `rounded-card` class
  </done>
</task>

</tasks>

<verification>
After task completes:
1. `npx tsc --noEmit` returns 0 errors
2. Search CityFilter.tsx for `bg-[#002147]` — found in tabActive
3. Search CityFilter.tsx for `text-[#002147]` — found in EventCard date paragraph
4. `rounded-card` still present on EventCard Link element
</verification>

<success_criteria>
- Events city filter tabs: active tab renders as Oxford blue pill (`bg-[#002147] text-white rounded-full`)
- Events card date: renders in Oxford blue (`text-[#002147]`) in light mode, `text-blue-300` in dark mode
- Event card corners: `rounded-card` class present (24px from Phase 20 token)
- TypeScript compiles cleanly
- No changes to event type chips, card hover states, grid layout, or page.tsx
</success_criteria>

<output>
After completion, create `.planning/phases/23-events-podcast-accent/23-01-SUMMARY.md`
</output>
