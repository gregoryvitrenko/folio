---
phase: 23-events-podcast-accent
plan: 02
type: execute
wave: 2
depends_on:
  - 23-01
files_modified:
  - components/PodcastPlayer.tsx
autonomous: true
requirements:
  - POD-01

must_haves:
  truths:
    - "The podcast hero background has a subtle Oxford blue ambient glow or gradient — visually premium and differentiated"
    - "The episode title and player controls remain fully readable — the glow does not overpower content"
    - "The dark stone-900 base background is preserved — the glow is a layered accent, not a replacement"
  artifacts:
    - path: "components/PodcastPlayer.tsx"
      provides: "Podcast player hero block with Oxford blue glow"
      contains: "#002147"
  key_links:
    - from: "PodcastPlayer hero div"
      to: "Oxford blue radial gradient layer"
      via: "absolute positioned div with radial gradient from-[#002147]/25"
      pattern: "#002147"
---

<objective>
Add a subtle Oxford blue ambient glow to the podcast hero block in PodcastPlayer.tsx — positioning it as a layered radial gradient behind the episode title and player controls.

Purpose: The podcast hero was redesigned in Phase 19 as an immersive dark stone-900 block. This plan adds the final v2 accent: an Oxford blue glow that signals premium without obscuring content.
Output: Updated components/PodcastPlayer.tsx with Oxford blue hero accent.
</objective>

<execution_context>
@/Users/gregoryvitrenko/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gregoryvitrenko/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

Design constraints:
- Oxford blue: `#002147`
- The glow MUST be subtle — episode title and controls must remain clearly readable
- Base background `bg-stone-900` stays — glow is an additive accent layer
- No emojis, no zinc in content areas

<interfaces>
From components/PodcastPlayer.tsx (current hero structure):

```tsx
{/* ── Hero block ─────────────────────────────────────────────────────── */}
<div className="bg-stone-900 text-stone-50">
  <div className="grid grid-cols-1 lg:grid-cols-[1fr_220px]">

    {/* Left: episode info + player */}
    <div className="px-8 pt-8 pb-7 flex flex-col gap-4">
      {/* ...episode title, player controls, waveform... */}
    </div>

    {/* Right: cover art card */}
    <div className="hidden lg:flex items-center justify-center p-6">
      {/* ...decorative card... */}
    </div>

  </div>
</div>
```

The hero outer div (`bg-stone-900`) is the insertion point.
The inner grid and its children must remain untouched — only the outer container gets the glow treatment.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Oxford blue radial gradient glow on podcast hero</name>
  <files>components/PodcastPlayer.tsx</files>
  <action>
Add a subtle Oxford blue ambient glow to the podcast hero block. Implementation:

Change the hero outer div from:
```tsx
<div className="bg-stone-900 text-stone-50">
```
To:
```tsx
<div className="relative bg-stone-900 text-stone-50 overflow-hidden">
```

Then add an absolutely-positioned glow layer as the FIRST child inside this div, before the grid:
```tsx
{/* Oxford blue ambient glow — subtle accent layer */}
<div
  className="pointer-events-none absolute inset-0 z-0"
  style={{
    background: 'radial-gradient(ellipse 70% 80% at 20% 50%, rgba(0,33,71,0.35) 0%, transparent 70%)',
  }}
  aria-hidden="true"
/>
```

Then add `relative z-10` to the grid div so content renders above the glow:
```tsx
<div className="grid grid-cols-1 lg:grid-cols-[1fr_220px] relative z-10">
```

Opacity/intensity notes:
- `rgba(0,33,71,0.35)` — Oxford blue at 35% opacity. This is the ceiling. If it looks too strong visually, the executor may reduce to 0.25. The text must remain fully readable.
- Ellipse positioned at `20% 50%` — radiates from the left side where the episode title sits, not behind the play button which is centred lower
- `70%` stop means the glow fades well before reaching the right cover-art panel

Do NOT modify: the controls strip (`bg-stone-950`), the briefing notes panel, the waveform bars, play button, progress bar, speed control, download button, or any JS logic. Only the hero outer div and grid div classNames change, plus the new glow div insertion.
  </action>
  <verify>
    <automated>npx tsc --noEmit --project /Users/gregoryvitrenko/Documents/Folio/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>
    - TypeScript compiles with no errors
    - PodcastPlayer.tsx hero outer div has `relative overflow-hidden`
    - PodcastPlayer.tsx contains `rgba(0,33,71` (Oxford blue in rgb) in the glow layer
    - The grid div has `relative z-10`
    - The glow div has `pointer-events-none absolute inset-0 z-0 aria-hidden="true"`
    - No changes to controls strip, briefing notes, or any JS logic
  </done>
</task>

</tasks>

<verification>
After task completes:
1. `npx tsc --noEmit` returns 0 errors
2. Search PodcastPlayer.tsx for `rgba(0,33,71` — found in glow layer
3. Hero outer div has `relative overflow-hidden`
4. Grid div has `relative z-10`
5. `bg-stone-900` still present on hero outer div (base background preserved)
</verification>

<success_criteria>
- Podcast hero: Oxford blue radial gradient glow present as absolute positioned layer
- Glow opacity: 25-35% — subtle, not overpowering
- Episode title and player controls: above the glow layer via `z-10`
- Base `bg-stone-900` hero background: preserved
- TypeScript compiles cleanly
- No functional changes to player behaviour
</success_criteria>

<output>
After completion, create `.planning/phases/23-events-podcast-accent/23-02-SUMMARY.md`
</output>
