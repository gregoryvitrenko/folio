---
phase: 13-typography-spacing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tailwind.config.ts
  - components/StoryCard.tsx
  - components/StoryGrid.tsx
  - components/ArticleStory.tsx
autonomous: true
requirements: [TYPO-01, TYPO-02, TYPO-03, TYPO-04, SPACE-01, SPACE-02]

must_haves:
  truths:
    - "Story card headlines render in Playfair Display serif at semibold (600) weight"
    - "Article page headlines render in Playfair Display serif at semibold (600) weight"
    - "Section/topic labels are legible at 11px minimum"
    - "Body text renders at 16px site-wide"
    - "Vertical gap between story cards in the grid is visibly more generous than before"
    - "Internal card spacing between headline, excerpt, and meta elements is comfortable, not cramped"
  artifacts:
    - path: "tailwind.config.ts"
      provides: "Updated type tokens: body 1rem, label 0.6875rem, article fontWeight 600"
      contains: "body.*1rem"
    - path: "components/StoryCard.tsx"
      provides: "Semibold serif headline with increased internal spacing"
      contains: "font-semibold"
    - path: "components/StoryGrid.tsx"
      provides: "Increased grid gap"
      contains: "gap-6"
    - path: "components/ArticleStory.tsx"
      provides: "Article headline at semibold weight via token"
  key_links:
    - from: "tailwind.config.ts"
      to: "components/StoryCard.tsx"
      via: "text-subheading token used on h2"
      pattern: "text-subheading"
    - from: "tailwind.config.ts"
      to: "components/ArticleStory.tsx"
      via: "text-article token used on h2 with baked fontWeight"
      pattern: "text-article"
    - from: "tailwind.config.ts"
      to: "app/globals.css"
      via: "text-label token used by .section-label class"
      pattern: "text-label"
---

<objective>
Update the design system type tokens and component classes to deliver editorial-quality typography and generous spacing across the site.

Purpose: Elevate Folio from "polished app" to "newspaper-quality editorial" — serif headlines at the right weight, legible labels, comfortable body text, and breathing room between cards.
Output: Updated tailwind.config.ts tokens, StoryCard/ArticleStory weight corrections, StoryGrid gap increase, card internal spacing bump.
</objective>

<execution_context>
@/Users/gregoryvitrenko/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gregoryvitrenko/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/13-typography-spacing/13-CONTEXT.md

<interfaces>
<!-- Key token definitions in tailwind.config.ts that will be updated -->

From tailwind.config.ts (theme.extend.fontSize):
```typescript
body:       ['0.9375rem', { lineHeight: '1.6'  }],                      // 15px — body copy
label:      ['0.625rem',  { lineHeight: '1', letterSpacing: '0.1em', fontWeight: '500' }], // 10px — mono labels
article:    ['1.75rem',   { lineHeight: '1.2', fontWeight: '700' }],     // 28px — article-level headline
subheading: ['1.125rem',  { lineHeight: '1.35', fontWeight: '500' }],    // 18px — subsection headings
```

From components/StoryCard.tsx line 59 (h2):
```tsx
<h2 className="font-serif text-subheading font-bold leading-snug text-stone-900 dark:text-stone-50 tracking-tight mb-3 group-hover:underline decoration-stone-400 dark:decoration-stone-500 underline-offset-2 pr-6">
```

From components/ArticleStory.tsx line 53 (h2):
```tsx
<h2 className="font-serif text-article leading-tight text-stone-900 dark:text-stone-50 tracking-tight mb-6">
```

From components/StoryGrid.tsx line 55 (grid container):
```tsx
<div className="grid grid-cols-1 lg:grid-cols-2 gap-4">
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update type tokens and headline weight</name>
  <files>tailwind.config.ts, components/StoryCard.tsx, components/ArticleStory.tsx</files>
  <action>
In tailwind.config.ts, update these fontSize tokens:

1. `body`: change `'0.9375rem'` (15px) to `'1rem'` (16px). Keep lineHeight `'1.6'`. No fontWeight.
2. `label`: change `'0.625rem'` (10px) to `'0.6875rem'` (11px). Keep lineHeight `'1'`, letterSpacing `'0.1em'`, fontWeight `'500'`.
3. `article`: change fontWeight from `'700'` to `'600'`. Keep size `'1.75rem'` and lineHeight `'1.2'`.

Do NOT change the `subheading` token — it is at fontWeight 500 which is lighter than semibold. The StoryCard h2 overrides it with an explicit font-weight class.

In components/StoryCard.tsx line 59:
- Change `font-bold` to `font-semibold` on the h2 element. This takes Playfair Display from 700 to 600 weight.

In components/ArticleStory.tsx:
- The h2 on line 53 uses `text-article` which now bakes fontWeight 600 via the token update. No class change needed — just verify it has no conflicting `font-bold` class (it does not).
- OPTIONAL cleanup (per Claude's Discretion): on line 58, change `text-[16px]` to `text-body` since the body token is now 16px. Keep the existing `leading-[1.75]` override. Same for line 101 (legacy whyItMatters) — change `text-[16px]` to `text-body`.
  </action>
  <verify>
    <automated>cd /Users/gregoryvitrenko/Documents/Folio && npx tsc --noEmit 2>&1 | head -20 && echo "---" && grep -n "font-semibold" components/StoryCard.tsx && echo "---" && grep -n "fontWeight.*600" tailwind.config.ts && echo "---" && grep -n "0.6875rem" tailwind.config.ts && echo "---" && grep -n "'1rem'" tailwind.config.ts</automated>
  </verify>
  <done>
- TypeScript compiles without errors
- StoryCard h2 has font-semibold (not font-bold)
- tailwind.config.ts article token has fontWeight 600
- tailwind.config.ts label token is 0.6875rem (11px)
- tailwind.config.ts body token is 1rem (16px)
  </done>
</task>

<task type="auto">
  <name>Task 2: Increase grid and card internal spacing</name>
  <files>components/StoryGrid.tsx, components/StoryCard.tsx</files>
  <action>
In components/StoryGrid.tsx line 55:
- Change `gap-4` to `gap-6` on the grid container. This increases the gap between story cards from 16px to 24px, giving the page room to breathe like a broadsheet.

In components/StoryCard.tsx, increase internal element spacing within the card article:
- Line 51: category label bottom margin — change `mb-3` to `mb-4` (12px to 16px)
- Line 59: headline bottom margin — change `mb-3` to `mb-4` (12px to 16px)
- Line 70: firm tags top margin — keep `mt-3` as-is (already has breathing room)
- Line 95: talking point teaser top margin/padding — change `mt-3 pt-3` to `mt-4 pt-4` (12px to 16px)
- Line 101: read more top margin — change `mt-4` to `mt-5` (16px to 20px)
- Card outer padding: change `px-5 pt-5 pb-6` to `px-5 pt-6 pb-7` on line 36 — slightly more vertical breathing room

These are +1 step increases that add comfort without making cards feel sparse.
  </action>
  <verify>
    <automated>cd /Users/gregoryvitrenko/Documents/Folio && grep -n "gap-6" components/StoryGrid.tsx && echo "---" && grep -n "mb-4" components/StoryCard.tsx && echo "---" && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>
- StoryGrid uses gap-6 for the card grid
- StoryCard internal margins increased by 1 step (mb-3 to mb-4 on category/headline, mt-3/pt-3 to mt-4/pt-4 on teaser)
- Card outer padding slightly increased (pt-6 pb-7)
- Build succeeds with no errors
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes — no type errors
2. `npx next build` succeeds — no build errors
3. Token values in tailwind.config.ts match requirements: body=1rem, label=0.6875rem, article fontWeight=600
4. StoryCard h2 uses `font-semibold` not `font-bold`
5. StoryGrid gap is `gap-6`
</verification>

<success_criteria>
- Story card headlines render in Playfair Display at semibold weight (600), visibly distinct from body Inter text
- Labels are legible at 11px — no squinting on any device
- Body text renders at 16px — comfortable for sustained reading
- Card grid has generous spacing (gap-6 = 24px between cards)
- Internal card spacing between headline, excerpt, and meta feels balanced — no cramped stacking
</success_criteria>

<output>
After completion, create `.planning/phases/13-typography-spacing/13-01-SUMMARY.md`
</output>
