---
phase: 05-utility-pages-analytics
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/archive/page.tsx
  - app/quiz/page.tsx
  - components/QuizInterface.tsx
autonomous: true
requirements:
  - UTIL-01
  - UTIL-03
must_haves:
  truths:
    - "Archive page list containers use rounded-card (2px) and stone palette — not the legacy rounded-xl/zinc pattern"
    - "Quiz page date list uses rounded-card and stone palette — consistent with archive"
    - "Page headings on both pages follow the canonical icon + text-subheading font-bold + .section-label badge pattern"
    - "Month headings and date-nav section labels use the .section-label class (not inline mono combos)"
    - "Emerald 'Today' badges are preserved unchanged (status colour, not palette)"
    - "QuizInterface at /quiz shows warm stone tones — no blue-grey zinc-coloured elements visible to users"
  artifacts:
    - path: "app/archive/page.tsx"
      provides: "Archive page — all zinc replaced with stone, rounded-xl replaced with rounded-card, font-mono inline combos replaced with .section-label"
      contains: "rounded-card"
    - path: "app/quiz/page.tsx"
      provides: "Quiz page — date list and heading migrated to stone tokens"
      contains: "rounded-card"
    - path: "components/QuizInterface.tsx"
      provides: "Quiz interaction component — all zinc palette replaced with stone, rounded-xl/lg replaced with rounded-card/chrome, arbitrary text sizes replaced with named tokens, hover:opacity removed"
      contains: "rounded-card"
  key_links:
    - from: "app/archive/page.tsx"
      to: "globals.css .section-label"
      via: "className section-label"
      pattern: "section-label"
    - from: "app/quiz/page.tsx"
      to: "tailwind.config.ts rounded-card"
      via: "rounded-card on div container"
      pattern: "rounded-card"
    - from: "components/QuizInterface.tsx"
      to: "tailwind.config.ts rounded-card / rounded-chrome"
      via: "rounded-card on answer containers, rounded-chrome on badges"
      pattern: "rounded-card"
---

<objective>
Migrate the Archive and Quiz pages from the pre-token zinc palette to the established stone/token system, and migrate the QuizInterface component that renders the actual quiz interaction at /quiz.

Purpose: Bring Archive (/archive) and Quiz (/quiz) into visual consistency with the rest of the product. QuizInterface has the heaviest violation count of any component in the codebase — it must be migrated alongside quiz/page.tsx or UTIL-03 cannot be satisfied.
Output: Three files with all zinc→stone replacements, rounded-xl→rounded-card, rounded-lg→rounded-chrome, arbitrary text sizes→named tokens, and hover:opacity removals applied.
</objective>

<execution_context>
@/Users/gregoryvitrenko/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gregoryvitrenko/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-utility-pages-analytics/05-RESEARCH.md

<interfaces>
<!-- Established token system — verified in tailwind.config.ts and globals.css during research -->

Token reference (Tailwind v3 — tailwind.config.ts):
- text-subheading: 18px / lineHeight 1.4 / fontWeight 500
- text-caption: 13px / lineHeight 1.5
- text-label: 10px / letterSpacing 0.08em
- rounded-card: 2px (editorial flat — replaces rounded-xl on content containers)
- rounded-chrome: 4px (replaces rounded or rounded-xl on badges/UI chrome; replaces rounded-lg on smaller chrome)

Component class (globals.css):
- .section-label: font-mono + text-label + tracking-widest + uppercase + text-stone-400
  → Use for: month headings, section nav labels, count badges (add bg/padding separately)
  → Do NOT add text-stone-400 explicitly when using .section-label (colour is encoded)
  → Do NOT use .section-label for the emerald "Today" badge (keep its own classes)

Current violations in app/archive/page.tsx (from source audit):
- line 49: className="text-zinc-400" on Calendar icon → text-stone-400
- line 50: text-lg font-bold text-zinc-900 dark:text-zinc-50 → text-subheading font-bold text-stone-900 dark:text-stone-50
- line 53: font-mono text-[10px] text-zinc-400 dark:text-zinc-500 tracking-widest uppercase bg-zinc-100 dark:bg-zinc-800 px-2 py-0.5 rounded → section-label bg-stone-100 dark:bg-stone-800 px-2 py-0.5 rounded-chrome
- line 60: text-zinc-500 dark:text-zinc-400 → text-stone-500 dark:text-stone-400
- line 63: text-zinc-400 dark:text-zinc-500 hover:text-zinc-600 dark:hover:text-zinc-300 → text-stone-400 dark:text-stone-500 hover:text-stone-600 dark:hover:text-stone-300
- line 72: font-mono text-[10px] tracking-widest uppercase text-zinc-400 dark:text-zinc-500 → section-label
- line 75: rounded-xl bg-white dark:bg-zinc-900 border border-zinc-200 dark:border-zinc-800 overflow-hidden divide-y divide-zinc-100 dark:divide-zinc-800 → rounded-card bg-white dark:bg-stone-900 border border-stone-200 dark:border-stone-800 overflow-hidden divide-y divide-stone-100 dark:divide-stone-800
- line 82: hover:bg-zinc-50 dark:hover:bg-zinc-800/50 → hover:bg-stone-50 dark:hover:bg-stone-800/50
- line 85: text-zinc-900 dark:text-zinc-100 ... group-hover:text-zinc-700 dark:group-hover:text-zinc-50 → text-stone-900 dark:text-stone-100 ... group-hover:text-stone-700 dark:group-hover:text-stone-50
- line 89: emerald badge — KEEP unchanged (status colour)
- line 96: text-zinc-300 dark:text-zinc-600 group-hover:text-zinc-500 dark:group-hover:text-zinc-400 → text-stone-300 dark:text-stone-600 group-hover:text-stone-500 dark:group-hover:text-stone-400
- Also find and fix any active row: bg-zinc-50 dark:bg-zinc-800/30 → bg-stone-50 dark:bg-stone-800/30

Current violations in app/quiz/page.tsx (from source audit):
- line 120: font-mono text-[10px] tracking-widest uppercase text-zinc-400 dark:text-zinc-500 → section-label
- line 123: rounded-xl bg-white dark:bg-zinc-900 border border-zinc-200 dark:border-zinc-800 overflow-hidden divide-y divide-zinc-100 dark:divide-zinc-800 → rounded-card bg-white dark:bg-stone-900 border border-stone-200 dark:border-stone-800 overflow-hidden divide-y divide-stone-100 dark:divide-stone-800
- line 128: emerald badge → KEEP unchanged
- line 135: bg-zinc-50 dark:bg-zinc-800/30 → bg-stone-50 dark:bg-stone-800/30
- line 137: text-zinc-900 dark:text-zinc-100 → text-stone-900 dark:text-stone-100
- line 150: hover:bg-zinc-50 dark:hover:bg-zinc-800/50 → hover:bg-stone-50 dark:hover:bg-stone-800/50
- line 153: text-zinc-900 dark:text-zinc-100 ... group-hover:text-zinc-700 dark:group-hover:text-zinc-50 → stone equivalents
- line 160: text-zinc-300 dark:text-zinc-600 ... → stone equivalents
- line 175-176: text-zinc-400, text-lg font-bold text-zinc-900 dark:text-zinc-50 → stone + text-subheading font-bold
- line 182: text-zinc-500 dark:text-zinc-400 → text-stone-500 dark:text-stone-400
- line 185: text-zinc-400 dark:text-zinc-500 hover:text-zinc-600 dark:hover:text-zinc-300 → stone equivalents
- line 208-212: text-zinc-400, text-lg font-bold text-zinc-900 dark:text-zinc-50, font-mono text-[10px] text-zinc-400 ... rounded → stone + text-subheading font-bold + section-label + rounded-chrome

Current violations in components/QuizInterface.tsx (from source audit — heaviest in codebase):
Zinc palette violations: 28x zinc-400, 25x zinc-500, 23x zinc-900, 9x zinc-50, 9x zinc-100, 9x zinc-800, 8x zinc-700, 5x zinc-300, 3x zinc-600
Border radius violations: 12x rounded-xl, 4x rounded-lg
Hover opacity violations: 5x hover:opacity (should be hover:bg-stone-* for explicit surface)
Arbitrary text sizes: text-[10px], text-[11px], text-[12px], text-[13px], text-[14px], text-[20px], text-[22px]

Replacement map for QuizInterface.tsx:
- All zinc-* → stone-* (same shade number — zinc-400 → stone-400, zinc-900 → stone-900, etc.)
- rounded-xl → rounded-card (content containers: answer cards, score panels, result cards)
- rounded-lg → rounded-chrome (smaller chrome: icon containers, badges, pill labels)
- hover:opacity-* → remove the opacity modifier; replace with hover:bg-stone-700 (dark) or hover:bg-stone-100 (light) as context requires
- text-[10px] → text-label (10px named token)
- text-[11px] → text-label (closest named token; 10px is close enough for labels)
- text-[12px] → text-label
- text-[13px] → text-caption (13px named token)
- text-[14px] → text-caption (14px maps to caption at 13px — acceptable; if font-size precision matters use text-sm as fallback)
- text-[20px] → text-subheading (18px) — only if it is a heading element; else text-xl
- text-[22px] → text-xl or text-2xl — pick whichever is structurally correct for the heading level
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate app/archive/page.tsx to design tokens</name>
  <files>app/archive/page.tsx</files>
  <action>
Read app/archive/page.tsx in full, then apply ALL of the following changes:

1. Page heading section (around line 49-53):
   - Calendar icon: text-zinc-400 → text-stone-400
   - h2: text-lg font-bold text-zinc-900 dark:text-zinc-50 → text-subheading font-bold text-stone-900 dark:text-stone-50 (keep tracking-tight)
   - Count badge span: replace the entire class string "font-mono text-[10px] text-zinc-400 dark:text-zinc-500 tracking-widest uppercase bg-zinc-100 dark:bg-zinc-800 px-2 py-0.5 rounded" with "section-label bg-stone-100 dark:bg-stone-800 px-2 py-0.5 rounded-chrome"

2. Empty state (around line 60-63):
   - "text-zinc-500 dark:text-zinc-400" → "text-stone-500 dark:text-stone-400"
   - "text-zinc-400 dark:text-zinc-500 hover:text-zinc-600 dark:hover:text-zinc-300" → "text-stone-400 dark:text-stone-500 hover:text-stone-600 dark:hover:text-stone-300"

3. Month heading h3 (around line 72):
   - Replace "font-mono text-[10px] tracking-widest uppercase text-zinc-400 dark:text-zinc-500" with "section-label" (keep mb-3 separately)

4. Date list container div (around line 75):
   - "rounded-xl bg-white dark:bg-zinc-900 border border-zinc-200 dark:border-zinc-800 overflow-hidden divide-y divide-zinc-100 dark:divide-zinc-800" → "rounded-card bg-white dark:bg-stone-900 border border-stone-200 dark:border-stone-800 overflow-hidden divide-y divide-stone-100 dark:divide-stone-800"

5. Active date row (if present — bg-zinc-50 dark:bg-zinc-800/30):
   - → "bg-stone-50 dark:bg-stone-800/30" (keep all other classes)

6. Date row link (around line 82):
   - "hover:bg-zinc-50 dark:hover:bg-zinc-800/50" → "hover:bg-stone-50 dark:hover:bg-stone-800/50"

7. Date row text span (around line 85):
   - "text-zinc-900 dark:text-zinc-100" → "text-stone-900 dark:text-stone-100"
   - "group-hover:text-zinc-700 dark:group-hover:text-zinc-50" → "group-hover:text-stone-700 dark:group-hover:text-stone-50"

8. Emerald "Today" badge (around line 89): LEAVE UNCHANGED — it is a status colour.

9. ChevronRight icon (around line 96):
   - "text-zinc-300 dark:text-zinc-600 group-hover:text-zinc-500 dark:group-hover:text-zinc-400" → "text-stone-300 dark:text-stone-600 group-hover:text-stone-500 dark:group-hover:text-stone-400"

After making all changes, scan the whole file for any remaining "zinc" class that is NOT inside the emerald Today badge. Fix any found.

Do NOT change any logic, imports, or structure — only className strings.
  </action>
  <verify>
    <automated>cd /Users/gregoryvitrenko/Documents/Folio && npm run lint 2>&1 | tail -20</automated>
  </verify>
  <done>app/archive/page.tsx has zero zinc class violations outside the emerald Today badge; all list containers use rounded-card; page heading uses text-subheading font-bold; count badge and month headings use .section-label class; lint passes</done>
</task>

<task type="auto">
  <name>Task 2: Migrate app/quiz/page.tsx to design tokens</name>
  <files>app/quiz/page.tsx</files>
  <action>
Read app/quiz/page.tsx in full, then apply ALL of the following changes:

1. Date-nav section label h3 (around line 120):
   - Replace "font-mono text-[10px] tracking-widest uppercase text-zinc-400 dark:text-zinc-500" with "section-label" (keep mb-3 separately)

2. Date list container div (around line 123):
   - "rounded-xl bg-white dark:bg-zinc-900 border border-zinc-200 dark:border-zinc-800 overflow-hidden divide-y divide-zinc-100 dark:divide-zinc-800" → "rounded-card bg-white dark:bg-stone-900 border border-stone-200 dark:border-stone-800 overflow-hidden divide-y divide-stone-100 dark:divide-stone-800"

3. Emerald "Today" badge (around line 128): LEAVE UNCHANGED.

4. Active date row div (around line 135):
   - "bg-zinc-50 dark:bg-zinc-800/30" → "bg-stone-50 dark:bg-stone-800/30"
   - Inner text span "text-zinc-900 dark:text-zinc-100" → "text-stone-900 dark:text-stone-100"

5. Link row (around line 150):
   - "hover:bg-zinc-50 dark:hover:bg-zinc-800/50" → "hover:bg-stone-50 dark:hover:bg-stone-800/50"

6. Link row text span (around line 153):
   - "text-zinc-900 dark:text-zinc-100" → "text-stone-900 dark:text-stone-100"
   - "group-hover:text-zinc-700 dark:group-hover:text-zinc-50" → "group-hover:text-stone-700 dark:group-hover:text-stone-50"

7. ChevronRight icon (around line 160):
   - "text-zinc-300 dark:text-zinc-600 group-hover:text-zinc-500 dark:group-hover:text-zinc-400" → "text-stone-300 dark:text-stone-600 group-hover:text-stone-500 dark:group-hover:text-stone-400"

8. Page heading (empty state section, around line 175-176):
   - Icon: "text-zinc-400" → "text-stone-400"
   - h2: "text-lg font-bold text-zinc-900 dark:text-zinc-50" → "text-subheading font-bold text-stone-900 dark:text-stone-50" (keep tracking-tight)

9. Empty state text (around line 182):
   - "text-zinc-500 dark:text-zinc-400" → "text-stone-500 dark:text-stone-400"

10. Empty state link (around line 185):
    - "text-zinc-400 dark:text-zinc-500 hover:text-zinc-600 dark:hover:text-zinc-300" → "text-stone-400 dark:text-stone-500 hover:text-stone-600 dark:hover:text-stone-300"

11. Main page heading (around line 208-212):
    - Icon: "text-zinc-400" → "text-stone-400"
    - h2: "text-lg font-bold text-zinc-900 dark:text-zinc-50" → "text-subheading font-bold text-stone-900 dark:text-stone-50" (keep tracking-tight)
    - Count badge: "font-mono text-[10px] text-zinc-400 dark:text-zinc-500 tracking-widest uppercase bg-zinc-100 dark:bg-zinc-800 px-2 py-0.5 rounded" → "section-label bg-stone-100 dark:bg-stone-800 px-2 py-0.5 rounded-chrome"

After changes, scan the whole file for any remaining "zinc" class. Fix any found.

Do NOT modify QuizInterface — that component is migrated in Task 3. Do NOT change any logic, imports, or structure.
  </action>
  <verify>
    <automated>cd /Users/gregoryvitrenko/Documents/Folio && npm run lint 2>&1 | tail -20</automated>
  </verify>
  <done>app/quiz/page.tsx has zero zinc class violations outside emerald Today badge; date list containers use rounded-card; all page headings use text-subheading font-bold; count badge and section labels use .section-label class; lint passes</done>
</task>

<task type="auto">
  <name>Task 3: Migrate components/QuizInterface.tsx to design tokens</name>
  <files>components/QuizInterface.tsx</files>
  <action>
Read components/QuizInterface.tsx in full. This component has the heaviest violation count in the codebase. Apply all of the following changes systematically.

**Step 1 — Zinc palette → stone palette (same shade number throughout):**
Do a full pass replacing every zinc class with the stone equivalent at the same shade:
- zinc-50 → stone-50
- zinc-100 → stone-100
- zinc-200 → stone-200
- zinc-300 → stone-300
- zinc-400 → stone-400
- zinc-500 → stone-500
- zinc-600 → stone-600
- zinc-700 → stone-700
- zinc-800 → stone-800
- zinc-900 → stone-900

Apply to ALL variant prefixes: plain, dark:, hover:, dark:hover:, group-hover:, dark:group-hover:, border-, bg-, text-, divide-, ring-, etc.

After this pass, scan the file for any remaining "zinc" string. Fix any missed.

**Step 2 — Border radius violations:**
- rounded-xl → rounded-card (for content containers: answer option cards, score panel, result card, quiz wrapper)
- rounded-lg → rounded-chrome (for smaller UI chrome: icon containers, pill labels, small badges)

Note: if rounded-lg is used on an answer option card (large interactive surface), use rounded-card instead.

**Step 3 — hover:opacity removal:**
Every instance of hover:opacity-* must be replaced with an explicit background hover:
- On dark answer option cards or dark surfaces: replace hover:opacity-80 (or similar) with hover:bg-stone-700
- On light surfaces or bordered cards: replace with hover:bg-stone-100 dark:hover:bg-stone-800
- Remove any paired transition-opacity if it was only serving the opacity hover

**Step 4 — Arbitrary text sizes → named tokens:**
- text-[10px] → text-label
- text-[11px] → text-label
- text-[12px] → text-label
- text-[13px] → text-caption
- text-[14px] → text-caption
- text-[20px] → text-subheading (if on a heading element) or text-xl (otherwise)
- text-[22px] → text-xl (if sub-heading level) or text-2xl (if primary heading)

Apply these replacements. Do NOT change font-weight, line-height, or letter-spacing — only the text-size class.

After all four steps, do a final scan of the whole file for:
1. Any remaining "zinc" string → fix
2. Any remaining "rounded-xl" or "rounded-lg" → fix
3. Any remaining "hover:opacity" → fix
4. Any remaining text-[Npx] → fix

Do NOT change any logic, event handlers, TypeScript types, or component structure — only className strings.
  </action>
  <verify>
    <automated>cd /Users/gregoryvitrenko/Documents/Folio && npm run lint 2>&1 | tail -20</automated>
  </verify>
  <done>components/QuizInterface.tsx has zero zinc violations; no rounded-xl or rounded-lg; no hover:opacity; no arbitrary text-[Npx] sizes; all containers use rounded-card or rounded-chrome; lint passes</done>
</task>

</tasks>

<verification>
After all three tasks complete, run: `cd /Users/gregoryvitrenko/Documents/Folio && npm run build 2>&1 | tail -30`

Build must exit 0 with no TypeScript errors.
</verification>

<success_criteria>
- app/archive/page.tsx: zero zinc violations (excluding emerald badge); rounded-card on list container; .section-label on month headings and count badge; lint clean
- app/quiz/page.tsx: zero zinc violations (excluding emerald badge); rounded-card on date list container; .section-label on section label and count badge; all heading instances use text-subheading font-bold; lint clean
- components/QuizInterface.tsx: zero zinc violations; no rounded-xl/rounded-lg; no hover:opacity; no arbitrary text-[Npx] sizes; rounded-card on content containers; rounded-chrome on chrome elements; lint clean
- npm run build passes
</success_criteria>

<output>
After completion, create `.planning/phases/05-utility-pages-analytics/05-01-SUMMARY.md`
</output>
