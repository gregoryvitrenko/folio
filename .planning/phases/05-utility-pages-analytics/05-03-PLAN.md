---
phase: 05-utility-pages-analytics
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/layout.tsx
  - app/upgrade/page.tsx
  - app/success/page.tsx
  - app/sign-up/[[...sign-up]]/page.tsx
  - app/sign-up/[[...sign-up]]/SignUpTracker.tsx (conditional — Approach B only)
autonomous: true
requirements:
  - ANLYT-01
  - ANLYT-02
must_haves:
  truths:
    - "Vercel Analytics records page views visible in the dashboard after production deployment"
    - "upgrade_view event fires once on mount when user visits /upgrade"
    - "checkout_click event fires in handleUpgrade() before the fetch('/api/stripe/checkout') call"
    - "subscription_activated event fires in success/page.tsx when verified transitions to true"
    - "free_signup event fires in sign-up page when user completes Clerk sign-up (isSignedIn transitions to true)"
  artifacts:
    - path: "app/layout.tsx"
      provides: "Analytics component mounted globally for all page view tracking"
      contains: "Analytics"
    - path: "app/upgrade/page.tsx"
      provides: "upgrade_view and checkout_click conversion events"
      contains: "track('upgrade_view')"
    - path: "app/success/page.tsx"
      provides: "subscription_activated conversion event"
      contains: "track('subscription_activated')"
    - path: "app/sign-up/[[...sign-up]]/page.tsx"
      provides: "free_signup conversion event via useUser() hook"
      contains: "track('free_signup')"
  key_links:
    - from: "app/layout.tsx"
      to: "@vercel/analytics/next"
      via: "import { Analytics } from '@vercel/analytics/next'"
      pattern: "Analytics"
    - from: "app/upgrade/page.tsx"
      to: "@vercel/analytics"
      via: "import { track } from '@vercel/analytics'"
      pattern: "track\\("
    - from: "app/success/page.tsx"
      to: "@vercel/analytics"
      via: "import { track } from '@vercel/analytics'"
      pattern: "track\\("
    - from: "app/sign-up/[[...sign-up]]/page.tsx"
      to: "@vercel/analytics"
      via: "track('free_signup') on sign-up completion (isSignedIn transition)"
      pattern: "track\\("
user_setup:
  - service: vercel-analytics
    why: "Analytics component requires Vercel Analytics to be enabled in the project dashboard before events are recorded"
    dashboard_config:
      - task: "Enable Vercel Analytics for the Folio project"
        location: "Vercel Dashboard → Folio project → Analytics tab → Enable"
---

<objective>
Install Vercel Analytics and wire all four conversion funnel events: free sign-up, upgrade page view, checkout click, and subscription activation. These changes are production-only — events appear in the Vercel Analytics dashboard after deployment.

Purpose: Make the conversion funnel observable so data can drive future CRO decisions (v2 requirement CRO-01 depends on this baseline).
Output: @vercel/analytics installed; <Analytics /> in layout; four track() calls in three client components.
</objective>

<execution_context>
@/Users/gregoryvitrenko/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gregoryvitrenko/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-utility-pages-analytics/05-RESEARCH.md

<interfaces>
<!-- Key patterns verified against Vercel Analytics docs (2026-03-09) -->

Package install:
  npm install @vercel/analytics

Import paths:
  import { Analytics } from '@vercel/analytics/next';  // layout.tsx — App Router
  import { track } from '@vercel/analytics';            // client components

Analytics component placement:
  // Place as last child of <body>, after <ScrollToTop />, outside <Providers>
  // WRONG: inside <Providers> — adds unnecessary re-render scope
  // RIGHT: direct child of <body>

Current app/layout.tsx structure (from source read):
  <body>
    <Providers>
      <main className="flex-1">{children}</main>
      <SiteFooter />
    </Providers>
    <ScrollToTop />   ← <Analytics /> goes AFTER this
  </body>

Current app/upgrade/page.tsx (from source read):
  - File already has 'use client' on line 1
  - handleUpgrade() function at line 23 — add track('checkout_click') before the fetch() call
  - No existing useEffect for upgrade_view — add one: useEffect(() => { track('upgrade_view'); }, [])
  - Import track from '@vercel/analytics' at the top of the file

Current app/success/page.tsx (from source read):
  - File already has 'use client' on line 1
  - verified state (boolean | null) defined at line 12
  - verified transitions to true via setTimeout at line 20
  - Add: useEffect(() => { if (verified === true) { track('subscription_activated'); } }, [verified])
  - Import track from '@vercel/analytics'

Current app/sign-up/[[...sign-up]]/page.tsx (from source read):
  - Currently a Server Component — no 'use client', no hooks
  - Add 'use client' directive at the top
  - Import { useUser } from '@clerk/nextjs' (already in codebase elsewhere)
  - Import { track } from '@vercel/analytics'
  - Import { useEffect } from 'react'
  - Add useEffect inside the component:
      useEffect(() => {
        if (isSignedIn) { track('free_signup'); }
      }, [isSignedIn]);
  - Get isSignedIn from const { isSignedIn } = useUser();
  - If adding 'use client' causes build errors (Clerk SSR conflict), extract a thin SignUpTracker
    client component instead: a component that uses useUser() and renders null, placed inside
    the existing JSX. This is the fallback approach.

Pitfall: track() is client-only. Do NOT call from Server Components. All four touch points are
client components — no server-side tracking needed.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install @vercel/analytics and add Analytics component to layout</name>
  <files>app/layout.tsx</files>
  <action>
Step 1 — Install the package:
Run: cd /Users/gregoryvitrenko/Documents/Folio && npm install @vercel/analytics

Step 2 — Update app/layout.tsx:
Read app/layout.tsx in full. Add the Analytics import at the top of the file:
  import { Analytics } from '@vercel/analytics/next';

Then place `<Analytics />` as the last child inside `<body>`, immediately after the existing `<ScrollToTop />` line:

Current structure:
  <body ...>
    <Providers>
      <main className="flex-1">{children}</main>
      <SiteFooter />
    </Providers>
    <ScrollToTop />
  </body>

Updated structure:
  <body ...>
    <Providers>
      <main className="flex-1">{children}</main>
      <SiteFooter />
    </Providers>
    <ScrollToTop />
    <Analytics />
  </body>

Do NOT place <Analytics /> inside <Providers> — it must be outside to avoid unnecessary re-renders.
Do NOT change any other part of layout.tsx.
  </action>
  <verify>
    <automated>cd /Users/gregoryvitrenko/Documents/Folio && npm run lint 2>&1 | tail -20 && grep -l 'Analytics' app/layout.tsx && grep -l 'track(' app/upgrade/page.tsx app/success/page.tsx 2>/dev/null || echo "track() not yet added — added in Task 2"</automated>
  </verify>
  <done>@vercel/analytics appears in package.json dependencies; Analytics component is imported and rendered in app/layout.tsx as a direct child of body, after ScrollToTop; lint passes</done>
</task>

<task type="auto">
  <name>Task 2: Add conversion event tracking to upgrade, success, and sign-up pages</name>
  <files>app/upgrade/page.tsx, app/success/page.tsx, app/sign-up/[[...sign-up]]/page.tsx</files>
  <action>
These are three separate file changes. Make each one independently.

--- app/upgrade/page.tsx ---
Read the file in full. It already has 'use client'.
1. Add to the import section: import { track } from '@vercel/analytics';
2. Add a useEffect hook inside the component (after the existing useState/useUser hooks):
   useEffect(() => {
     track('upgrade_view');
   }, []);
3. In the handleUpgrade function (around line 23), add track('checkout_click') as the FIRST line of the function body, before the try/catch:
   async function handleUpgrade() {
     track('checkout_click');
     // ... existing try/catch with loading state and fetch ...
   }

--- app/success/page.tsx ---
Read the file in full. It already has 'use client'.
1. Add to the import section: import { track } from '@vercel/analytics';
2. Add a new useEffect after the existing useEffect that sets verified:
   useEffect(() => {
     if (verified === true) {
       track('subscription_activated');
     }
   }, [verified]);

--- app/sign-up/[[...sign-up]]/page.tsx ---
Read the file in full. It is currently a Server Component.
Approach A (preferred): Add 'use client' directly to the file.
1. Add 'use client'; as the first line of the file
2. Add imports: import { useEffect } from 'react'; and import { useUser } from '@clerk/nextjs'; and import { track } from '@vercel/analytics';
3. Inside the component function, before the return statement:
   const { isSignedIn } = useUser();
   useEffect(() => {
     if (isSignedIn) {
       track('free_signup');
     }
   }, [isSignedIn]);

If Approach A causes a build error (check with npm run lint / npm run build), use Approach B:
Create a thin client component file at app/sign-up/[[...sign-up]]/SignUpTracker.tsx:
  'use client';
  import { useEffect } from 'react';
  import { useUser } from '@clerk/nextjs';
  import { track } from '@vercel/analytics';
  export function SignUpTracker() {
    const { isSignedIn } = useUser();
    useEffect(() => {
      if (isSignedIn) { track('free_signup'); }
    }, [isSignedIn]);
    return null;
  }
Then import and render <SignUpTracker /> inside the existing sign-up page JSX (keep page.tsx as a Server Component).
  </action>
  <verify>
    <automated>cd /Users/gregoryvitrenko/Documents/Folio && npm run lint 2>&1 | tail -20 && grep -l 'Analytics' app/layout.tsx && grep -l 'track(' app/upgrade/page.tsx app/success/page.tsx</automated>
  </verify>
  <done>All three files import track from '@vercel/analytics'; upgrade_view and checkout_click calls are in upgrade/page.tsx; subscription_activated fires when verified===true in success/page.tsx; free_signup fires when isSignedIn in sign-up page; lint passes</done>
</task>

</tasks>

<verification>
After both tasks complete, run: `cd /Users/gregoryvitrenko/Documents/Folio && npm run build 2>&1 | tail -30`

Build must exit 0. TypeScript must accept all track() calls.

Note: Event recording requires the Vercel Analytics dashboard to be enabled (user_setup action) AND the build to be deployed. Production verification is in Plan 05-04.
</verification>

<success_criteria>
- @vercel/analytics in package.json dependencies
- Analytics component rendered in app/layout.tsx (outside Providers, after ScrollToTop)
- track('upgrade_view') fires on mount in app/upgrade/page.tsx
- track('checkout_click') fires in handleUpgrade() before fetch() in app/upgrade/page.tsx
- track('subscription_activated') fires when verified===true in app/success/page.tsx
- track('free_signup') fires when isSignedIn in sign-up flow
- npm run build passes
</success_criteria>

<output>
After completion, create `.planning/phases/05-utility-pages-analytics/05-03-SUMMARY.md`
</output>
