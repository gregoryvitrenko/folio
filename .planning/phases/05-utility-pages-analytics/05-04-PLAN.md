---
phase: 05-utility-pages-analytics
plan: "04"
type: execute
wave: 2
depends_on:
  - "05-01"
  - "05-02"
  - "05-03"
files_modified: []
autonomous: false
requirements:
  - UTIL-01
  - UTIL-02
  - UTIL-03
  - UTIL-04
  - UTIL-05
  - ANLYT-01
  - ANLYT-02
must_haves:
  truths:
    - "All five utility pages read as the same product as the core briefing pages — no page looks out of place"
    - "Vercel Analytics is enabled in the project dashboard and recording page views"
    - "Conversion funnel events appear in the Vercel Analytics Events panel after testing the flows"
  artifacts: []
  key_links:
    - from: "Vercel Analytics dashboard"
      to: "app/layout.tsx <Analytics />"
      via: "production deploy + dashboard enable"
      pattern: "n/a — manual check"
---

<objective>
Visual and functional verification checkpoint: confirm all five utility pages are visually correct after deployment, and verify Vercel Analytics is active and recording conversion events.

Purpose: Close the feedback loop. Plans 05-01 through 05-03 are code-only; this plan confirms the production result is correct before the milestone is marked complete.
Output: All Phase 5 success criteria confirmed in production.
</objective>

<execution_context>
@/Users/gregoryvitrenko/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gregoryvitrenko/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Push all changes and wait for Vercel deploy</name>
  <what-built>
    Plans 05-01, 05-02, and 05-03 have produced code changes across eight files:
    - app/archive/page.tsx — zinc→stone migration
    - app/quiz/page.tsx — zinc→stone migration
    - components/QuizInterface.tsx — full token migration (zinc, rounded, hover:opacity, arbitrary sizes)
    - app/firms/page.tsx — zinc badge and heading migration
    - app/tests/page.tsx — rounded-chrome and text-caption fixes
    - app/primers/page.tsx — rounded-chrome and text-caption fixes
    - app/layout.tsx — Analytics component added
    - app/upgrade/page.tsx, app/success/page.tsx, app/sign-up page — conversion event tracking

    @vercel/analytics has been installed (package.json and package-lock.json updated).
  </what-built>
  <how-to-verify>
    Push all committed changes to trigger the Vercel production deploy:

    1. Open Terminal.app (NOT Claude Code — dev server must not run here)
    2. Run: cd /Users/gregoryvitrenko/Documents/Folio && git status
       Confirm all modified files from Plans 05-01, 05-02, 05-03 are committed.
       If any are uncommitted, stage and commit them now:
         git add app/archive/page.tsx app/quiz/page.tsx components/QuizInterface.tsx \
           app/firms/page.tsx app/tests/page.tsx app/primers/page.tsx \
           app/layout.tsx app/upgrade/page.tsx app/success/page.tsx \
           "app/sign-up/[[...sign-up]]/page.tsx" package.json package-lock.json
         git commit -m "feat(05): apply token system to utility pages, install Vercel Analytics"
    3. Run: git push origin main
    4. Wait for the Vercel deploy to complete.
       Check status: gh run list --limit 3
       Or visit: https://vercel.com/dashboard → Folio project → Deployments
    5. Confirm the deploy status shows "Ready" before proceeding to Task 2.
  </how-to-verify>
  <resume-signal>Type "deployed" once the Vercel deploy shows "Ready", or describe any deploy errors</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual and analytics verification</name>
  <what-built>
    Plans 05-01 and 05-02 migrated five utility pages to the established design token system:
    - /archive: zinc to stone, rounded-xl to rounded-card on date list, .section-label on headings and badges
    - /quiz: zinc to stone, rounded-xl to rounded-card on date list, .section-label on headings and badges; QuizInterface fully migrated
    - /firms: zinc to stone on badge and heading, rounded-sm to rounded-card on disclaimer
    - /tests: rounded to rounded-chrome on badge, text-[13px] to text-caption on body text
    - /primers: rounded to rounded-chrome on badge, text-[13px] to text-caption on body text

    Plan 05-03 installed @vercel/analytics with four conversion events:
    - free_signup (sign-up completion)
    - upgrade_view (upgrade page load)
    - checkout_click (Subscribe button click)
    - subscription_activated (success page confirmation)
  </what-built>
  <how-to-verify>
    **Step 1 — Enable Vercel Analytics in the dashboard (if not already done)**
    Visit: https://vercel.com/dashboard → Folio project → Analytics tab → click Enable
    This must be done before events will be recorded.

    **Step 2 — Visual check of all 5 utility pages**
    Visit each page on the live site at both 375px (mobile) and 1280px (desktop):
    - https://www.folioapp.co.uk/archive
    - https://www.folioapp.co.uk/quiz
    - https://www.folioapp.co.uk/firms
    - https://www.folioapp.co.uk/tests
    - https://www.folioapp.co.uk/primers

    For each page, confirm:
    - [ ] Page heading: icon (muted grey) + bold title + small monospace badge — all stone colour
    - [ ] Any list containers have very flat corners (2px) — not the legacy large rounded corners
    - [ ] Count badges have 4px corners — look crisp, not pill-shaped
    - [ ] No grey elements that look noticeably blue-grey vs the warm stone palette of other pages
    - [ ] Archive and Quiz date list rows have stone-coloured text and hover states
    - [ ] Quiz interaction at /quiz: answer options use stone colours, no blue-grey tones

    **Step 3 — Trigger conversion events and verify in dashboard**
    a. Visit https://www.folioapp.co.uk/upgrade (triggers upgrade_view)
    b. Click "Subscribe" (triggers checkout_click — you can close the Stripe page without paying)
    c. If you have a test account, complete a sign-up (triggers free_signup)
    d. Wait 30-60 seconds, then visit: Vercel Dashboard → Folio project → Analytics → Events tab
    e. Confirm upgrade_view and checkout_click events are visible

    **Step 4 — Confirm page view tracking**
    In Vercel Analytics dashboard, confirm the Analytics tab shows page view data after your visits.
  </how-to-verify>
  <action>Human visual and dashboard verification — no automated action</action>
  <verify>
    <automated>cd /Users/gregoryvitrenko/Documents/Folio && git log --oneline -3</automated>
  </verify>
  <done>All five utility pages pass visual inspection; Vercel Analytics dashboard shows page views and at least upgrade_view + checkout_click events</done>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
Verification is the checkpoint itself. All five utility pages visually consistent with the core product. Vercel Analytics dashboard showing page view data and at least upgrade_view + checkout_click events.
</verification>

<success_criteria>
- All five utility pages pass visual inspection at 375px and 1280px
- QuizInterface at /quiz shows warm stone palette — no blue-grey zinc elements
- Vercel Analytics enabled in the dashboard and recording page views
- At least upgrade_view and checkout_click events visible in the Events tab
- Phase 5 complete — milestone "Market-Ready Design Lift" is done
</success_criteria>

<output>
After completion, create `.planning/phases/05-utility-pages-analytics/05-04-SUMMARY.md`
</output>
